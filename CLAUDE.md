# NeoRecall contributor context

Read `AGENTS.md`, `GUIDELINES.md`, and `docs/docs/architecture.md` before changing ingest or transcript receipt behavior. Backend code is CommonJS with strict mode. HTTP routes only validate and translate requests; services own business logic.
