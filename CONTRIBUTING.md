# Contributing

Use Node.js 20 or newer and Flutter stable. Run `npm test`, `npm run flutter:analyze`, and `npm run flutter:test` before opening a pull request. Security-sensitive changes to authentication, audio lifecycle, user isolation, or biometric voiceprints require regression tests.

By contributing, you agree that your work is licensed under AGPL-3.0-only.
