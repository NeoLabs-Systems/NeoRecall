<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window the Compose app draws into. Colours themselves come from the
         Compose theme; this only stops the system flashing its own background
         between the launcher and the first frame. -->
    <style name="Theme.NeoRecall.Wear" parent="android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:colorAccent">#E1B052</item>
        <!-- Wear's DeviceDefault window is translucent. A translucent activity
             can swallow the microphone prompt and fail the microphone
             foreground-service start that follows a grant. -->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowSwipeToDismiss">false</item>
    </style>

    <!-- A tile tap must not flash a screen on its way to the microphone. -->
    <style name="Theme.NeoRecall.Wear.Invisible" parent="android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoDisplay">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
