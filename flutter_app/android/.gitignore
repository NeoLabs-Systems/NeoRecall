gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Committed on purpose: fixed debug-signing key so release APKs built in CI
# keep a stable signature across builds and can be installed as updates.
!app/debug.keystore
